---
phase: 02-operational-safety
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - layouts/404.html
autonomous: true
requirements:
  - A11Y-03
tags:
  - 404
  - error-page
  - branded-chrome
  - design-system
must_haves:
  truths:
    - "A 404 hit on the production site renders the CING header and footer"
    - "The 404 page offers three navigation choices: home, news, councillors"
    - "The 404 page tone is branded confident with light Cornish flavour, not generic page-not-found copy"
    - "GitHub Pages serves Hugo's emitted public/404.html on any 404"
  artifacts:
    - path: "layouts/404.html"
      provides: "Branded 404 layout that inherits header.html + footer.html via define main"
      contains: "{{ define \"main\" }}"
    - path: "layouts/404.html"
      provides: "Three CTAs to home, news, councillors"
      contains: "/news/"
  key_links:
    - from: "layouts/404.html"
      to: "header partial"
      via: "baseof.html includes header.html in default block"
      pattern: "define \"main\""
    - from: "Hugo build"
      to: "GitHub Pages 404 serving"
      via: "public/404.html"
      pattern: "public/404.html"
---

<objective>
Create a branded `layouts/404.html` so a 404 hit on `www.cingparty.uk` renders the CING header, footer, and three primary navigation CTAs instead of GitHub's default error page. Tone-band: branded confident with Cornish flavour (per CONTEXT D-09 — *not* generic "Sorry, page not found", *not* deadpan "404").

Purpose: Closes REQ A11Y-03. Hugo emits `public/404.html` automatically when a `layouts/404.html` exists; GitHub Pages serves that file on any 404 with no Pages config change required.

Output: One new file `layouts/404.html` composed of three existing patterns (template shell + editorial header + bento CTA cards) per PATTERNS §8. No other files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-operational-safety/02-CONTEXT.md
@.planning/phases/02-operational-safety/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@layouts/_default/baseof.html
@layouts/partials/header.html
@layouts/partials/footer.html
@layouts/index.html
@layouts/get-involved/list.html
@docs/brand/design-principles.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create layouts/404.html composed from existing patterns</name>
  <files>layouts/404.html</files>
  <read_first>
    - layouts/_default/baseof.html (full file — confirm `{{ block "main" . }}` block at approx :207, header partial inclusion at :207, footer partial at :213)
    - layouts/partials/header.html (full file — confirm it provides the glassmorphism nav and is self-contained)
    - layouts/partials/footer.html (full file — confirm it provides the 3-column footer)
    - layouts/index.html lines 1-15 (template-shell pattern: `{{ define "main" }}` opening) and lines 65-115 (principles bento CTA cards — colour-rhythm reference)
    - layouts/get-involved/list.html lines 1-20 (editorial-header pattern: heading + description sat in a flex row)
    - .planning/phases/02-operational-safety/02-CONTEXT.md §"Branded 404" (D-08 contents requirement, D-09 tone band)
    - .planning/phases/02-operational-safety/02-PATTERNS.md §8 ("layouts/404.html — branded error page") — full composite skeleton
    - .planning/codebase/CONVENTIONS.md §"Hugo Templating Conventions" (`{{ define "main" }}` is the inherit-baseof pattern; no `hugo.IsProduction` gate for content surfaces)
    - docs/brand/design-principles.md (no 1px borders, sections separated by tonal surface shifts, editorial-shadow on cards)
  </read_first>
  <action>
    Create a NEW file at `layouts/404.html` (NOT `layouts/_default/404.html` — the Hugo special path is at the layouts root). Use this exact file body, derived from PATTERNS §8 composite skeleton with the executor's own tone-band-compliant wording for the headline, sub-headline, and three CTA descriptions:

    ```go-html
    {{ define "main" }}

    <div class="pt-32 pb-20">
      {{/* ============================================
            EDITORIAL HEADER — 404 announcement
           ============================================ */}}
      <header class="max-w-screen-2xl mx-auto px-6 lg:px-8 mb-12">
        <div class="flex flex-col md:flex-row gap-12 items-end">
          <div class="max-w-3xl">
            <span class="font-label text-tertiary uppercase tracking-widest text-sm font-bold block mb-4">404 — Not Found</span>
            <h1 class="font-headline text-4xl md:text-6xl lg:text-8xl font-extrabold tracking-tight text-primary leading-[0.9]">
              This page has<br/>wandered off<br/><span class="text-tertiary">the map.</span>
            </h1>
          </div>
          <div class="pb-4">
            <p class="text-on-surface-variant max-w-md font-body leading-relaxed text-lg">
              The link may be old, or the page may have moved. Try one of these instead.
            </p>
          </div>
        </div>
      </header>

      {{/* ============================================
            THREE CTAs — home, news, councillors
           ============================================ */}}
      <section class="max-w-screen-2xl mx-auto px-6 lg:px-8 grid grid-cols-1 md:grid-cols-3 gap-6">
        <a href="{{ "/" | relURL }}" class="bg-primary text-white p-8 rounded-xl flex flex-col justify-between min-h-[180px] hover:bg-primary-container transition-colors group">
          <span class="material-symbols-outlined text-tertiary-fixed text-4xl">home</span>
          <div>
            <h2 class="font-headline font-bold text-2xl mb-1">Home</h2>
            <p class="text-on-primary-container text-sm">Back to the front page.</p>
          </div>
        </a>
        <a href="{{ "/news/" | relURL }}" class="bg-surface-container-lowest editorial-shadow p-8 rounded-xl flex flex-col justify-between min-h-[180px] hover:bg-surface-container transition-colors">
          <span class="material-symbols-outlined text-primary text-4xl">newspaper</span>
          <div>
            <h2 class="font-headline font-bold text-2xl text-primary mb-1">Latest News</h2>
            <p class="text-on-surface-variant text-sm">What CING councillors are working on.</p>
          </div>
        </a>
        <a href="{{ "/councillors/" | relURL }}" class="bg-tertiary-container p-8 rounded-xl flex flex-col justify-between min-h-[180px] hover:bg-tertiary hover:text-white transition-colors">
          <span class="material-symbols-outlined text-on-tertiary-container text-4xl">groups</span>
          <div>
            <h2 class="font-headline font-bold text-2xl text-on-tertiary-container mb-1">Your Councillors</h2>
            <p class="text-on-tertiary-container text-sm">Meet the independent voices for Cornwall.</p>
          </div>
        </a>
      </section>
    </div>

    {{ end }}
    ```

    Notes for the executor:
    - **Do NOT** wrap the body in `{{ if hugo.IsProduction }}`. CONTEXT §"Established Patterns" is explicit: 404 is content, not analytics.
    - **Do NOT** add `{{ partial "header.html" . }}` or `{{ partial "footer.html" . }}` inside this file — `baseof.html` already includes them via the `{{ block "main" . }}` mechanism. Adding them here would double-render the chrome.
    - **Do NOT** add a search box, recent-news teaser, or group-introduction blurb. These are explicitly out of scope per CONTEXT D-08 ("No search box, no recent-news teaser, no group-introduction blurb — those would be scope creep or belong on a different surface").
    - The `pt-32` matches the fixed-nav offset used by `layouts/get-involved/list.html:3` and `layouts/news/list.html:3`. Without it, the editorial header would sit under the glassmorphism nav.
    - The three CTAs use the homepage principles bento colour rhythm: navy `bg-primary` → surface `bg-surface-container-lowest editorial-shadow` → tertiary `bg-tertiary-container`. This is the canonical CING three-card cadence (CONVENTIONS §"Tertiary gold" — use sparingly).
    - Material Symbols (`home`, `newspaper`, `groups`) are already loaded by `baseof.html:174` — no extra `<link>` needed.
    - The headline cadence "This page has / wandered off / the map." follows the "Local Voice. / Local Action." two-part rhythm from CONVENTIONS. The executor MAY substitute alternative wording within the tone band per CONTEXT D-09, but if doing so MUST: stay in three short clauses, end the third clause with the tertiary-gold accent span, and avoid generic "Sorry, page not found" or deadpan "404" registers. Document any wording change in the SUMMARY with rationale.
  </action>
  <verify>
    <automated>test -f layouts/404.html &amp;&amp; grep -F '{{ define "main" }}' layouts/404.html &amp;&amp; grep -F '{{ end }}' layouts/404.html &amp;&amp; grep -F '/news/' layouts/404.html &amp;&amp; grep -F '/councillors/' layouts/404.html &amp;&amp; grep -F '/' layouts/404.html &amp;&amp; ! grep -F 'hugo.IsProduction' layouts/404.html &amp;&amp; ! grep -F 'partial "header.html"' layouts/404.html &amp;&amp; ! grep -F 'partial "footer.html"' layouts/404.html &amp;&amp; hugo --renderToMemory</automated>
  </verify>
  <acceptance_criteria>
    - `test -f layouts/404.html` exits 0
    - `grep -c '{{ define "main" }}' layouts/404.html` returns 1
    - `grep -c '{{ end }}' layouts/404.html` returns 1
    - `grep -c '"/" | relURL' layouts/404.html` returns 1 (home CTA)
    - `grep -c '"/news/" | relURL' layouts/404.html` returns 1 (news CTA)
    - `grep -c '"/councillors/" | relURL' layouts/404.html` returns 1 (councillors CTA)
    - `grep -c '404 — Not Found' layouts/404.html` returns 1 (eyebrow label)
    - `grep -c 'text-tertiary' layouts/404.html` returns at least 2 (eyebrow + headline accent)
    - `grep -c 'editorial-shadow' layouts/404.html` returns at least 1 (surface CTA card)
    - `grep -c 'material-symbols-outlined' layouts/404.html` returns 3 (one icon per CTA)
    - `grep -c 'hugo.IsProduction' layouts/404.html` returns 0 (no production gate on content)
    - `grep -c 'partial "header.html"' layouts/404.html` returns 0 (avoids double-rendering chrome)
    - `grep -c 'partial "footer.html"' layouts/404.html` returns 0
    - `hugo --renderToMemory` exits 0 (template parses)
  </acceptance_criteria>
  <done>layouts/404.html exists, contains the editorial header + three CTAs in correct colour rhythm, defines `main` block (inherits baseof), passes Hugo template parsing, and contains no production gate or duplicate-partial calls.</done>
</task>

<task type="auto">
  <name>Task 2: Build site, verify public/404.html renders correctly with branded chrome</name>
  <files>(no files modified — verification only; record results in SUMMARY)</files>
  <read_first>
    - layouts/404.html (post-Task-1 state)
    - layouts/partials/header.html (so the executor knows what to grep for in rendered output — pick a stable string like the CING wordmark)
    - layouts/partials/footer.html (likewise — pick a stable string from the footer)
    - .planning/phases/02-operational-safety/02-CONTEXT.md §"Code Context" — Integration points for 404 (Hugo emits public/404.html, GH Pages serves it)
  </read_first>
  <action>
    1. Build the site:

       ```bash
       hugo --gc --minify --baseURL "http://localhost:1313/"
       ```

       Confirm exit 0 and that `public/404.html` was created:

       ```bash
       test -f public/404.html && echo "404 emitted" || { echo "MISSING public/404.html"; exit 1; }
       ```

    2. Confirm the rendered 404 page contains branded chrome (header + footer):

       ```bash
       # Pick one stable string from header.html — typically a wordmark or logo class.
       # Read header.html first to identify the right grep target. Examples that should
       # always be present include the site title or the navigation menu items.
       grep -c 'CING' public/404.html      # expect at least 1 — site name appears in nav and/or footer
       grep -c 'Get Involved' public/404.html   # expect at least 1 — main nav item
       grep -c 'Cornish Independent' public/404.html  # expect at least 1 — footer or copyright line
       ```

       The exact strings depend on what the partials render — the executor reads `header.html` and `footer.html` to pick three stable, distinguishing strings (one from header nav, one from footer copyright, one from footer columns) and confirms each appears at least once in `public/404.html`. Document the chosen strings in the SUMMARY.

    3. Confirm the three CTAs render with correct hrefs:

       ```bash
       grep -c 'href="/"' public/404.html         # home CTA
       grep -c 'href="/news/"' public/404.html    # news CTA
       grep -c 'href="/councillors/"' public/404.html  # councillors CTA
       ```

       Each must return at least 1 (note: the home `href="/"` may appear additionally in the header nav; that's fine — at least 1 is the minimum).

    4. Confirm the editorial header renders with the apology copy:

       ```bash
       grep -c '404 — Not Found' public/404.html
       grep -c 'wandered off' public/404.html  # or whatever wording the executor chose
       ```

    5. Spin up `hugo server` and curl the 404 path to confirm GH-Pages-equivalent behaviour locally:

       ```bash
       hugo server --baseURL http://localhost:1313/ --disableFastRender &
       SERVER_PID=$!
       sleep 3
       curl -sI http://localhost:1313/this-page-definitely-does-not-exist-404-test/ | head -1
       # Expected: "HTTP/1.1 404 Not Found"
       curl -s http://localhost:1313/this-page-definitely-does-not-exist-404-test/ | grep -c '404 — Not Found'
       # Expected: 1 (the eyebrow label is rendered)
       kill $SERVER_PID
       ```

       Note: `hugo server` returns 404 status for unmatched paths and serves `public/404.html` content. This emulates GH Pages behaviour. Document the curl status line and grep result in the SUMMARY.

    6. Confirm no `hugo.IsProduction` leak in rendered output (the page must work in dev too):

       ```bash
       # Build with --environment development, confirm 404 still renders
       hugo --environment development --baseURL "http://localhost:1313/"
       test -f public/404.html && grep -c 'wandered off' public/404.html
       # Expected: 1 (page renders identically in dev)
       ```
  </action>
  <verify>
    <automated>hugo --gc --minify --baseURL "http://localhost:1313/" &amp;&amp; test -f public/404.html &amp;&amp; grep -c 'CING' public/404.html &amp;&amp; grep -c '404 — Not Found' public/404.html &amp;&amp; grep -c 'href="/news/"' public/404.html &amp;&amp; grep -c 'href="/councillors/"' public/404.html</automated>
  </verify>
  <acceptance_criteria>
    - `hugo --gc --minify` exits 0
    - `public/404.html` exists after build
    - `grep -c 'CING' public/404.html` returns at least 1 (proves header or footer chrome rendered)
    - `grep -c '404 — Not Found' public/404.html` returns 1 (proves editorial header rendered)
    - `grep -c 'href="/news/"' public/404.html` returns at least 1
    - `grep -c 'href="/councillors/"' public/404.html` returns at least 1
    - `grep -c 'href="/"' public/404.html` returns at least 1
    - The three executor-chosen header/footer chrome strings each grep-match at least once in `public/404.html`
    - `curl -I` against a non-existent path on `hugo server` returns `HTTP/1.1 404 Not Found`
    - `curl` body for the same path includes the editorial-header eyebrow `404 — Not Found`
    - Dev-environment build also produces an equivalent `public/404.html` (proves no `hugo.IsProduction` gating regression)
  </acceptance_criteria>
  <done>public/404.html is emitted, contains branded chrome and the three CTAs, returns 404 status when curl'd against a non-existent path on hugo server, and renders identically in dev and production environments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. The 404 page is a static HTML surface with three internal-link CTAs, all using `relURL`. No user input, no form, no dynamic content.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | n/a | layouts/404.html | n/a | Static page with internal-link CTAs only. No injection vectors, no untrusted input, no client-side state. The standard CSP and TLS protections from the rest of the site apply unchanged |
| T-02-04-02 | I (information disclosure) — open-redirect via 404 page | Three CTA links | accept | All three CTAs use `{{ "/path" | relURL }}` — Hugo emits site-relative URLs; no externally-controlled redirect target. Open-redirect surface is zero |
| T-02-04-03 | T (tampering) — wrong base layout chosen and chrome missing | Hugo template inheritance | mitigate | Task 2 step 2 grep-confirms header and footer strings render in `public/404.html`. If `baseof.html` is missing or the `{{ define "main" }}` block name is wrong, the chrome would not appear and the verification would fail |

No security-relevant threats. Block-on-high gate satisfied — this is a content surface change.
</threat_model>

<verification>
Plan-wide checks (run after both tasks):

1. **File exists:** `test -f layouts/404.html` exits 0
2. **Defines main block:** `grep -c '{{ define "main" }}' layouts/404.html` returns 1
3. **Three CTAs present:** `grep -c '/news/' layouts/404.html` and `grep -c '/councillors/' layouts/404.html` and `grep -c '"/" | relURL' layouts/404.html` each return at least 1
4. **No production gate:** `grep -c 'hugo.IsProduction' layouts/404.html` returns 0
5. **No duplicate partial calls:** `grep -c 'partial "header.html"' layouts/404.html` and `grep -c 'partial "footer.html"' layouts/404.html` both return 0
6. **Hugo build emits public/404.html:** after `hugo`, `test -f public/404.html` exits 0
7. **Rendered output has branded chrome:** `grep -c 'CING' public/404.html` returns at least 1
8. **Live 404 status:** `curl -I http://localhost:1313/<nonexistent-path>` returns `HTTP/1.1 404 Not Found` against `hugo server`
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion #7 ("A 404 hit on the production site renders the CING header, footer, and a navigation choice instead of GitHub's default error") satisfied.
- Both tasks pass acceptance criteria.
- Local `curl` against a non-existent path returns 404 status with a body containing the apology copy and three CTAs.
- Tone-band check: the rendered headline matches CONTEXT D-09 ("branded confident with Cornish flavour, not generic 'Sorry, page not found', not deadpan '404'").
- No GitHub Pages config change required — Hugo's emitted `public/404.html` is served by GH Pages automatically.
</success_criteria>

<output>
After completion, create `.planning/phases/02-operational-safety/02-04-SUMMARY.md` with:
- Final headline wording chosen (if executor deviated from PATTERNS §8 within tone band, justify here)
- The three header/footer chrome strings the executor chose for verification, plus their grep-counts in `public/404.html`
- `curl -I` status line from the live 404 spot-check
- `curl` body grep proof that the eyebrow label rendered
- Whether the dev-environment build also emitted an equivalent 404 (proves no production-only regression)
- Note any deviations from PATTERNS §8 composite skeleton with rationale (e.g. wording choices, layout adjustments)
</output>
