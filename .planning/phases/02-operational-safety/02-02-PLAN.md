---
phase: 02-operational-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hugo.toml
  - layouts/get-involved/list.html
  - layouts/index.html
  - layouts/press/list.html
autonomous: false
requirements:
  - FORMS-01
  - FORMS-02
  - FORMS-03
tags:
  - forms
  - formspree
  - gdpr
  - pecr
  - consent
user_setup:
  - service: formspree
    why: "Per-form endpoint split (FORMS-03) — three new Formspree forms required, one per submission stream"
    env_vars: []
    dashboard_config:
      - task: "Create three new Formspree forms"
        location: "Formspree Dashboard → New form (×3): Contact, Newsletter, Press"
        notes: "Copy the resulting form IDs into hugo.toml under params.formspreeContactId, formspreeNewsletterId, formspreePressId. The legacy params.formspreeId stays as an alias during migration."
must_haves:
  truths:
    - "Every Formspree-backed form has a hidden _gotcha honeypot input"
    - "Both newsletter forms refuse submission without the marketing_consent checkbox ticked"
    - "The press list form refuses submission without its journalist-consent checkbox ticked"
    - "Contact, newsletter, and press submissions route to three distinct Formspree endpoints"
  artifacts:
    - path: "hugo.toml"
      provides: "Three new Formspree endpoint params"
      contains: "formspreeContactId"
    - path: "hugo.toml"
      provides: "Newsletter endpoint param"
      contains: "formspreeNewsletterId"
    - path: "hugo.toml"
      provides: "Press endpoint param"
      contains: "formspreePressId"
    - path: "layouts/get-involved/list.html"
      provides: "Contact form with honeypot + new endpoint; newsletter form with honeypot + consent checkbox + new endpoint"
      contains: "_gotcha"
    - path: "layouts/index.html"
      provides: "Homepage newsletter form with honeypot + consent checkbox + newsletter endpoint"
      contains: "marketing_consent"
    - path: "layouts/press/list.html"
      provides: "Press list form with honeypot + journalist-consent checkbox + press endpoint"
      contains: "marketing_consent"
  key_links:
    - from: "form action attributes"
      to: "hugo.toml params"
      via: ".Site.Params.formspree*Id"
      pattern: "formspree\\.io/f/.*formspree(Contact|Newsletter|Press)Id"
    - from: "newsletter and press forms"
      to: "submission validation"
      via: "required checkbox"
      pattern: 'name="marketing_consent".*required'
---

<objective>
Make all four Formspree-backed forms compliant with PECR explicit-consent and basic spam protection. After this plan:
1. Each form has a hidden `_gotcha` honeypot (FORMS-01).
2. Both newsletter forms (and the press-list form) carry an unticked-by-default `required` opt-in checkbox with verbatim consent label per CONTEXT D-04 / D-05 (FORMS-02).
3. Contact / newsletter / press submissions route to three distinct Formspree IDs stored in `hugo.toml` (FORMS-03).

Purpose: PECR/UK-GDPR explicit-positive-action compliance plus inbox separation so press queries don't drown in newsletter signups. Aligns with the Phase-1 PECR precedent (script-load denial, not Consent Mode v2 default-deny).

Output: Modified `hugo.toml` with three new params (placeholder IDs initially), and four edited form blocks across three layout files. The plan is **non-autonomous** because a maintainer-prerequisite task is required to populate real Formspree IDs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-operational-safety/02-CONTEXT.md
@.planning/phases/02-operational-safety/02-PATTERNS.md
@hugo.toml
@layouts/get-involved/list.html
@layouts/index.html
@layouts/press/list.html
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Maintainer creates three Formspree forms and provides IDs</name>
  <files>(none — Formspree dashboard work; IDs eventually land in hugo.toml via Task 2)</files>
  <read_first>
    - .planning/phases/02-operational-safety/02-CONTEXT.md §"Form compliance" (D-05 — Manual prerequisite: maintainer creates three forms; legacy `formspreeId` stays as alias during migration)
    - .planning/phases/02-operational-safety/02-PATTERNS.md §2 (the three new params and where they go in hugo.toml)
  </read_first>
  <action>
    This is a checkpoint task — Claude does NOT execute it; the human maintainer does. The executor's job is to PAUSE here, surface the instructions below to the user, and wait for the resume signal.

    **Maintainer instructions:**

    1. Log in to Formspree at https://formspree.io/forms.
    2. Create THREE new forms:
       - **Contact** — for the contact form on `/get-involved/`
       - **Newsletter** — shared by the homepage newsletter form AND the get-involved newsletter form
       - **Press** — for the press-list signup on `/press/`
    3. For each form, copy the 8-character form ID (the suffix in `https://formspree.io/f/<id>`).
    4. Provide the three IDs to the executor by either:
       - Writing them directly into `hugo.toml` (replacing the `REPLACE_WITH_*` placeholders Task 2 inserts), OR
       - Pasting them in the resume signal so the executor edits `hugo.toml` after resuming.

    Either approach is fine. The executor must NOT proceed to Task 5 (verify forms render in browser) until real IDs are in place — placeholder IDs cause Formspree 404 on submit.

    **What-built:** Nothing in the repo at this checkpoint — this is the manual prerequisite per CONTEXT D-05. The actual `hugo.toml` edit happens in Task 2 (which runs once IDs are available, even if as placeholders for now).

    **How-to-verify (for the maintainer):** Confirm in the Formspree dashboard that three new forms exist and that you've recorded their 8-character IDs.

    **Resume-signal:** "Provide the three Formspree IDs (or confirm you've written them into hugo.toml). Format: contact=abcd1234, newsletter=efgh5678, press=ijkl9012 — or paste 'placeholder' if you want the plan to proceed with REPLACE_WITH_* values and you'll wire real IDs before merge."
  </action>
  <verify>
    <automated>echo "MANUAL — checkpoint task. Verification deferred to maintainer; resume-signal in Task 1 description gates progression."</automated>
  </verify>
  <done>Maintainer has either (a) provided three real Formspree IDs to be substituted in Task 2, or (b) explicitly authorised proceeding with `REPLACE_WITH_*` placeholders on the understanding that real IDs MUST be wired before merge to main. Either authorisation unblocks Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Add three Formspree endpoint params to hugo.toml</name>
  <files>hugo.toml</files>
  <read_first>
    - hugo.toml (current `[params]` block — confirm `formspreeId = "mvzvgdbl"` location near line 8)
    - .planning/phases/02-operational-safety/02-CONTEXT.md §"Form compliance" (D-Claude's-discretion FORMS-03 — three params, legacy alias retained)
    - .planning/phases/02-operational-safety/02-PATTERNS.md §2 ("hugo.toml endpoint split") — exact replacement block
  </read_first>
  <action>
    In `hugo.toml`, replace the existing `[params]` block such that the new state matches PATTERNS.md §2 exactly:

    ```toml
    [params]
      description = "The Cornish Independent NonAligned Group — independent councillors serving Cornwall's unique needs on Cornwall Council."
      # Formspree endpoints. Three forms in the dashboard, three IDs here.
      # Legacy single-endpoint id retained as alias until migration completes.
      formspreeId = "mvzvgdbl"
      formspreeContactId = "REPLACE_WITH_CONTACT_FORM_ID"
      formspreeNewsletterId = "REPLACE_WITH_NEWSLETTER_FORM_ID"
      formspreePressId = "REPLACE_WITH_PRESS_FORM_ID"
      ogImage = "/images/og-default.jpg"
    ```

    If Task 1 already supplied real IDs, substitute them directly in place of the `REPLACE_WITH_*` strings. If Task 1 deferred, leave the placeholders — the executor MUST replace them before the final `hugo` build verification step.

    Notes:
    - Do NOT remove `formspreeId = "mvzvgdbl"` (it remains as alias per CONTEXT D-Claude's-discretion FORMS-03).
    - Do NOT change `description` or `ogImage`.
    - Do NOT touch any other top-level keys (`baseURL`, `languageCode`, `title`, `copyright`, `[pagination]`, `[markup]`, etc.).
  </action>
  <verify>
    <automated>grep -c 'formspreeContactId' hugo.toml &amp;&amp; grep -c 'formspreeNewsletterId' hugo.toml &amp;&amp; grep -c 'formspreePressId' hugo.toml &amp;&amp; grep -c 'formspreeId = "mvzvgdbl"' hugo.toml &amp;&amp; hugo config &gt; /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'formspreeContactId' hugo.toml` returns 1
    - `grep -c 'formspreeNewsletterId' hugo.toml` returns 1
    - `grep -c 'formspreePressId' hugo.toml` returns 1
    - `grep -c 'formspreeId = "mvzvgdbl"' hugo.toml` returns 1 (legacy alias retained)
    - `hugo config | grep -i formspree` lists all four params (confirms TOML parses)
    - No other top-level keys removed (verifiable: `hugo config | grep -E '^(baseurl|languagecode|title|copyright)'` still returns the four pre-existing values)
  </acceptance_criteria>
  <done>hugo.toml has the four formspree* keys; legacy alias retained; `hugo config` succeeds; if maintainer provided real IDs, placeholders are replaced.</done>
</task>

<task type="auto">
  <name>Task 3: Update layouts/get-involved/list.html — contact form (honeypot + endpoint swap) and newsletter form (honeypot + consent checkbox + endpoint swap)</name>
  <files>layouts/get-involved/list.html</files>
  <read_first>
    - layouts/get-involved/list.html (full file — both form blocks at approx :108 contact and :155 newsletter; confirm exact line numbers as the file may have shifted)
    - .planning/phases/02-operational-safety/02-CONTEXT.md §"Form compliance" — D-04 verbatim newsletter consent label
    - .planning/phases/02-operational-safety/02-PATTERNS.md §3 ("Form pattern — honeypot + consent + endpoint swap") — exact HTML for contact, newsletter, and press forms; per-form endpoint mapping table
  </read_first>
  <action>
    Two distinct form edits in this file. Locate by `<form action=` (there should be exactly two in the file post-edit, matching the two pre-existing).

    **Edit A — Contact form** (currently at ~line 108, opens with `<form action="https://formspree.io/f/{{ .Site.Params.formspreeId }}" method="POST" class="space-y-6">`):

    1. Change the `action` attribute to `https://formspree.io/f/{{ .Site.Params.formspreeContactId }}`.
    2. Insert this honeypot input as the FIRST child element inside the `<form>` (before any existing `<div>` or `<input>`):

       ```html
       {{/* Honeypot — bots fill this; real users never see it */}}
       <input type="text" name="_gotcha" style="display:none" tabindex="-1" autocomplete="off" aria-hidden="true" />
       ```

    3. Do NOT add a `marketing_consent` checkbox to the contact form. Per CONTEXT D-04 + REQ FORMS-02, the consent checkbox is required only on newsletter and press forms. The contact form is transactional (the user is sending a message; submission itself implies they want a reply).
    4. Do NOT alter the existing footer paragraph `<p class="text-xs text-on-surface-variant text-center opacity-70">By submitting, you agree to our <a href="...">privacy policy</a>...</p>` — that's transactional-context language and stays as-is.

    **Edit B — Newsletter form** (currently at ~line 155, opens with `<form action="https://formspree.io/f/{{ .Site.Params.formspreeId }}" method="POST" class="flex flex-col sm:flex-row gap-4 max-w-lg mx-auto">`):

    1. Change the `action` attribute to `https://formspree.io/f/{{ .Site.Params.formspreeNewsletterId }}`.
    2. Insert the honeypot input immediately after the existing `<input type="hidden" name="_subject" value="Newsletter signup" />` line:

       ```html
       <input type="text" name="_gotcha" style="display:none" tabindex="-1" autocomplete="off" aria-hidden="true" />
       ```

    3. Restructure the form's outer flex layout to `flex flex-col gap-4 max-w-lg mx-auto` (the consent checkbox doesn't fit alongside email + button on one row). Then add the email row + button as a nested flex `<div class="flex flex-col sm:flex-row gap-4">` so the existing horizontal layout for email-and-subscribe is preserved at sm-and-up.
    4. Insert this exact consent label IMMEDIATELY BEFORE the `<button type="submit">Subscribe</button>` (verbatim per CONTEXT D-04 — do NOT paraphrase):

       ```html
       {{/* PECR explicit-consent — checkbox unticked by default, required to submit */}}
       <label class="flex items-start gap-3 text-sm text-on-surface-variant max-w-lg mx-auto">
         <input type="checkbox" name="marketing_consent" value="yes" required
           class="mt-1 shrink-0" />
         <span>
           I agree to receive occasional email updates from CING. You can unsubscribe at any time.
           See our <a href="{{ "/privacy/" | relURL }}" class="underline">privacy policy</a>.
         </span>
       </label>
       ```

       Note: this label uses `text-on-surface-variant` because the get-involved newsletter section sits on a light surface (verify by reading the parent `<section>` background — should be `bg-surface-container-low` or similar). If the parent is dark, swap to the white-text variant from PATTERNS §3 (`text-white/90` + `border-white/30 bg-white/10` on the input + `text-tertiary-fixed` on the link). Choose by reading the section's background class; do not guess.
    5. Do NOT pre-tick the checkbox. The `value="yes"` attribute is metadata, not state. Pre-ticked is forbidden per CONTEXT specifics ("PECR compliance precedent").
  </action>
  <verify>
    <automated>grep -c 'formspreeContactId' layouts/get-involved/list.html &amp;&amp; grep -c 'formspreeNewsletterId' layouts/get-involved/list.html &amp;&amp; [ "$(grep -c '_gotcha' layouts/get-involved/list.html)" -ge 2 ] &amp;&amp; grep -c 'name="marketing_consent"' layouts/get-involved/list.html &amp;&amp; grep -F 'I agree to receive occasional email updates from CING' layouts/get-involved/list.html &amp;&amp; ! grep -E 'formspreeId[^A-Za-z]' layouts/get-involved/list.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'formspreeContactId' layouts/get-involved/list.html` returns 1
    - `grep -c 'formspreeNewsletterId' layouts/get-involved/list.html` returns 1
    - `grep -c '_gotcha' layouts/get-involved/list.html` returns at least 2 (one per form)
    - `grep -c 'name="marketing_consent"' layouts/get-involved/list.html` returns 1 (newsletter only — NOT the contact form)
    - `grep -F 'I agree to receive occasional email updates from CING. You can unsubscribe at any time.' layouts/get-involved/list.html` exits 0 (verbatim D-04 wording)
    - `grep -F 'See our <a href="{{ "/privacy/" | relURL }}" class="underline">privacy policy</a>.' layouts/get-involved/list.html` exits 0
    - `grep -E 'formspreeId[^A-Za-z]' layouts/get-involved/list.html` returns 0 (legacy bare param removed from THIS file's form actions; alias only lives in hugo.toml)
    - `hugo --renderToMemory` exits 0 (template still parses)
  </acceptance_criteria>
  <done>Both forms have honeypots; newsletter form has the verbatim D-04 consent label with required unticked-by-default checkbox; both forms point at their new respective endpoint params; the legacy `formspreeId` reference is removed from this file's form actions.</done>
</task>

<task type="auto">
  <name>Task 4: Update layouts/index.html (homepage newsletter form) and layouts/press/list.html (press list form)</name>
  <files>layouts/index.html, layouts/press/list.html</files>
  <read_first>
    - layouts/index.html (full file — newsletter form near :205, plus context for the surrounding section's background colour)
    - layouts/press/list.html (full file — press list signup form near :147)
    - .planning/phases/02-operational-safety/02-CONTEXT.md §"Form compliance" (D-04 newsletter label, D-05 press label — both verbatim)
    - .planning/phases/02-operational-safety/02-PATTERNS.md §3 — exact HTML for both forms
  </read_first>
  <action>
    **Edit A — `layouts/index.html` homepage newsletter form** (currently at ~line 205, action `formspree.io/f/{{ .Site.Params.formspreeId }}`):

    1. Change the `action` attribute to `https://formspree.io/f/{{ .Site.Params.formspreeNewsletterId }}`.
    2. Insert honeypot input immediately after the existing `<input type="hidden" name="_subject" value="Newsletter signup" />`:

       ```html
       <input type="text" name="_gotcha" style="display:none" tabindex="-1" autocomplete="off" aria-hidden="true" />
       ```

    3. Insert the verbatim D-04 consent label IMMEDIATELY BEFORE the existing `<button type="submit">Subscribe</button>`. Use the dark-section variant (homepage newsletter sits on a navy `bg-primary` band — confirm by reading the parent section's class):

       ```html
       {{/* PECR explicit-consent — checkbox unticked by default, required to submit */}}
       <label class="flex items-start gap-3 text-sm text-white/90 max-w-lg mx-auto">
         <input type="checkbox" name="marketing_consent" value="yes" required
           class="mt-1 shrink-0 rounded border-white/30 bg-white/10 focus:ring-tertiary" />
         <span>
           I agree to receive occasional email updates from CING. You can unsubscribe at any time.
           See our <a href="{{ "/privacy/" | relURL }}" class="underline text-tertiary-fixed">privacy policy</a>.
         </span>
       </label>
       ```

    4. As in Task 3 Edit B: restructure the form layout so consent label fits — outer `flex flex-col gap-4`, inner `flex flex-col sm:flex-row gap-4` for email + button.

    **Edit B — `layouts/press/list.html` press list form** (currently at ~line 147):

    1. Change the `action` attribute to `https://formspree.io/f/{{ .Site.Params.formspreePressId }}`.
    2. Insert honeypot immediately after any existing `<input type="hidden" name="_subject" ...>` (or as the first child if no hidden subject exists — verify by reading the file):

       ```html
       <input type="text" name="_gotcha" style="display:none" tabindex="-1" autocomplete="off" aria-hidden="true" />
       ```

       Add `<input type="hidden" name="_subject" value="Press newsletter signup" />` if it's not already present, before the honeypot.

    3. Insert the verbatim D-05 press-specific consent label IMMEDIATELY BEFORE the existing `<button class="..." type="submit">Subscribe</button>` (or whatever the submit button reads). Press list sits on a light/surface band — use the light-surface variant:

       ```html
       {{/* PECR explicit-consent — press contacts only */}}
       <label class="flex items-start gap-3 text-sm text-on-surface-variant">
         <input type="checkbox" name="marketing_consent" value="yes" required
           class="mt-1 shrink-0" />
         <span>
           I'm a journalist or press contact and would like to receive CING press releases.
           See our <a href="{{ "/privacy/" | relURL }}" class="underline">privacy policy</a>.
         </span>
       </label>
       ```

    Notes for both edits:
    - Section-background detection: read the parent `<section>` `class` attribute; if it includes `bg-primary`, `bg-primary-container`, or any other dark token, use the `text-white/90` variant; otherwise use `text-on-surface-variant`. Document the choice in the SUMMARY.
    - Do NOT pre-tick checkboxes.
    - Do NOT change copy — the wording in D-04 and D-05 is locked.
    - Do NOT remove the existing footer privacy paragraph (if present) — the consent checkbox is additional, not replacement.
  </action>
  <verify>
    <automated>grep -c 'formspreeNewsletterId' layouts/index.html &amp;&amp; grep -c 'formspreePressId' layouts/press/list.html &amp;&amp; grep -c '_gotcha' layouts/index.html &amp;&amp; grep -c '_gotcha' layouts/press/list.html &amp;&amp; grep -c 'marketing_consent' layouts/index.html &amp;&amp; grep -c 'marketing_consent' layouts/press/list.html &amp;&amp; grep -F "I agree to receive occasional email updates from CING" layouts/index.html &amp;&amp; grep -F "I'm a journalist or press contact and would like to receive CING press releases" layouts/press/list.html &amp;&amp; hugo --renderToMemory</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'formspreeNewsletterId' layouts/index.html` returns 1
    - `grep -c 'formspreePressId' layouts/press/list.html` returns 1
    - `grep -c '_gotcha' layouts/index.html` returns at least 1
    - `grep -c '_gotcha' layouts/press/list.html` returns at least 1
    - `grep -c 'name="marketing_consent"' layouts/index.html` returns 1
    - `grep -c 'name="marketing_consent"' layouts/press/list.html` returns 1
    - `grep -F 'I agree to receive occasional email updates from CING. You can unsubscribe at any time.' layouts/index.html` exits 0
    - `grep -F "I'm a journalist or press contact and would like to receive CING press releases." layouts/press/list.html` exits 0
    - `grep -F '"/privacy/" | relURL' layouts/index.html` and `grep -F '"/privacy/" | relURL' layouts/press/list.html` both exit 0 (link target preserved)
    - `grep -E 'formspreeId[^A-Za-z]' layouts/index.html` returns 0
    - `grep -E 'formspreeId[^A-Za-z]' layouts/press/list.html` returns 0
    - `hugo --renderToMemory` exits 0 (both templates parse and render)
  </acceptance_criteria>
  <done>Homepage newsletter has D-04 verbatim label + honeypot + newsletter endpoint. Press list form has D-05 verbatim label + honeypot + press endpoint. Both checkboxes are unticked by default and `required`. Hugo build passes.</done>
</task>

<task type="auto">
  <name>Task 5: Build site, verify forms render with real Formspree IDs, run security probes</name>
  <files>(no files modified — verification only; record results in SUMMARY)</files>
  <read_first>
    - hugo.toml (confirm real Formspree IDs are in place — placeholders MUST be replaced after Task 1)
    - layouts/get-involved/list.html, layouts/index.html, layouts/press/list.html (post-Tasks-3-and-4 state)
    - .planning/phases/02-operational-safety/02-CONTEXT.md §"Form compliance" (full block — re-read to confirm acceptance interpretation)
  </read_first>
  <action>
    1. Build the site locally:

       ```bash
       hugo --gc --minify --baseURL "http://localhost:1313/"
       ```

       Confirm exit 0.

    2. Inspect the rendered HTML for each form by grepping the `public/` directory:

       ```bash
       grep -r 'formspree.io/f/' public/ | grep -v 'mvzvgdbl'
       ```

       Expected: at least four hits (one per form), each pointing at one of the three new IDs (or the placeholder strings if the maintainer hasn't supplied real IDs yet — document either way).

    3. Verify honeypots are present in rendered output:

       ```bash
       grep -c '_gotcha' public/get-involved/index.html
       grep -c '_gotcha' public/index.html
       grep -c '_gotcha' public/press/index.html
       ```

       All must return at least 1.

    4. Verify consent checkboxes render:

       ```bash
       grep -c 'marketing_consent' public/get-involved/index.html
       grep -c 'marketing_consent' public/index.html
       grep -c 'marketing_consent' public/press/index.html
       ```

       All must return 1 (each page has exactly one such form).

    5. Visual confirmation (executor reads the rendered HTML, doesn't need a browser): the consent checkbox `<input>` must NOT have `checked` or `checked="checked"` anywhere in the form output. Run:

       ```bash
       grep -E '<input[^>]*marketing_consent[^>]*checked' public/ -r
       ```

       Must return 0 hits.

    6. Confirm the legacy `formspreeId` alias still parses but is no longer referenced from any rendered form:

       ```bash
       grep -F 'mvzvgdbl' public/ -r
       ```

       Expected: 0 hits in `public/` (the alias is in `hugo.toml` but no template references it any more).

    Document all command outputs in the SUMMARY.

    **If the maintainer has not yet provided real Formspree IDs** (Task 1 deferred), DO NOT mark this task complete — the verification command at step 2 will show `REPLACE_WITH_*` placeholders, and the form would 404 on submit in production. Instead, leave a clear note in the SUMMARY: "Awaiting Formspree IDs from maintainer — placeholders still in hugo.toml. Final verification deferred."
  </action>
  <verify>
    <automated>hugo --gc --minify --baseURL "http://localhost:1313/" &amp;&amp; [ "$(grep -c '_gotcha' public/get-involved/index.html)" -ge 1 ] &amp;&amp; [ "$(grep -c '_gotcha' public/index.html)" -ge 1 ] &amp;&amp; [ "$(grep -c '_gotcha' public/press/index.html)" -ge 1 ] &amp;&amp; [ "$(grep -c 'marketing_consent' public/get-involved/index.html)" -eq 1 ] &amp;&amp; [ "$(grep -c 'marketing_consent' public/index.html)" -eq 1 ] &amp;&amp; [ "$(grep -c 'marketing_consent' public/press/index.html)" -eq 1 ] &amp;&amp; ! grep -rE '<input[^>]*marketing_consent[^>]*checked' public/ &amp;&amp; ! grep -rF 'mvzvgdbl' public/</automated>
  </verify>
  <acceptance_criteria>
    - `hugo --gc --minify` exits 0
    - All three rendered pages contain `_gotcha`: at least 1 hit each
    - All three rendered pages contain exactly 1 `marketing_consent` input
    - Zero rendered checkbox inputs have `checked` attribute
    - Zero rendered references to legacy `mvzvgdbl` ID in `public/`
    - Three distinct Formspree IDs (or placeholders) in `public/` form actions — one per stream
  </acceptance_criteria>
  <done>Hugo build clean; all three rendered forms carry honeypots; both newsletter forms and the press form carry unticked-by-default `required` consent checkboxes; legacy `mvzvgdbl` no longer rendered into output. SUMMARY records whether real IDs are in place or still placeholders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor browser → Formspree HTTPS endpoint | All four forms POST to `formspree.io` over TLS; data leaves the static site at submission |
| Visitor browser → CING `localStorage` | Cookie-consent state is stored client-side (existing — not changed by this plan) |
| Spam bot → form action | Honeypot field separates real submissions from automated fills |
| Newsletter consent state → Formspree submission record | The `marketing_consent` field accompanies each submission; Formspree's submission log is the canonical consent record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | T (tampering) — bot fills `_gotcha` to spam | All four forms | mitigate | Hidden `_gotcha` input; Formspree drops submissions where this field is non-empty (built-in feature). `tabindex="-1"` and `aria-hidden="true"` keep keyboard users and screen readers off it |
| T-02-02-02 | I (information disclosure) — newsletter consent not recorded | Newsletter and press forms | mitigate | `marketing_consent=yes` field included in every newsletter/press submission; Formspree's submission record provides time-stamped consent log per UK GDPR Art 7(1) |
| T-02-02-03 | S (spoofing) — pre-ticked or implicit consent | Newsletter and press forms | mitigate | Checkbox is unticked by default and carries `required`; PECR explicit-positive-action satisfied. Verification step 5 in Task 5 grep-confirms no `checked` attribute renders |
| T-02-02-04 | I (information disclosure) — legacy single-endpoint inbox sees press queries | Formspree dashboard | mitigate | Three distinct form IDs route press / newsletter / contact to separate inboxes; legacy `mvzvgdbl` alias retained in config but no template references it after this plan |
| T-02-02-05 | E (elevation) — placeholder IDs deployed to production | hugo.toml at deploy time | mitigate | Task 5 verification grep blocks deploy until placeholders are replaced; SUMMARY explicitly records the IDs-in-place vs placeholders status |
| T-02-02-06 | R (repudiation) — user disputes signing up | Newsletter signup record | accept | Formspree's submission timestamp + IP + form fields constitute the audit trail. No additional double-opt-in this phase (deferred — see CONTEXT §"Deferred Ideas: Switch newsletter to a real ESP") |
| T-02-02-07 | D (denial of service) — Formspree free-tier rate limit | All forms | accept | Free-tier rate limit (50/month per form) is the documented Formspree constraint. At current site traffic, this is well under threshold. Splitting into three forms increases per-form headroom |
| T-02-02-08 | T (tampering) — pre-existing transactional copy "By submitting, you agree…" misread as implicit-opt-in for marketing | Contact form footer | accept | Contact form is transactional (no `marketing_consent` checkbox added — per CONTEXT D-04 + REQ FORMS-02 explicitly newsletter-only). The copy refers to privacy-policy acceptance, not marketing consent. No PECR exposure |

All `high`-severity items mitigated. T-02-02-05 is the most consequential — Task 5's gating prevents placeholder leakage to prod.
</threat_model>

<verification>
Plan-wide checks (run after all tasks):

1. **All four endpoints distinct in TOML:** `grep -c 'formspree.*Id' hugo.toml` returns at least 4 (Contact, Newsletter, Press, plus legacy alias)
2. **All four forms have honeypots:** `grep -rc '_gotcha' layouts/get-involved/list.html layouts/index.html layouts/press/list.html` totals at least 4
3. **Three consent checkboxes:** `grep -rc 'name="marketing_consent"' layouts/` returns 3 (one in get-involved, one in index, one in press list)
4. **Zero pre-ticked:** `grep -rE '<input[^>]*marketing_consent[^>]*checked' layouts/` returns 0
5. **Verbatim D-04 wording:** the exact string "I agree to receive occasional email updates from CING. You can unsubscribe at any time." appears in both `layouts/get-involved/list.html` and `layouts/index.html`
6. **Verbatim D-05 wording:** "I'm a journalist or press contact and would like to receive CING press releases." appears in `layouts/press/list.html`
7. **Hugo build clean:** `hugo --renderToMemory` exits 0
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion #3 ("Every form on the site has a `_gotcha` honeypot field") satisfied across all four forms.
- ROADMAP Phase 2 success criterion #4 ("Both newsletter forms refuse submission without an explicit opt-in tickbox checked") satisfied — checkboxes are `required` and unticked by default.
- ROADMAP Phase 2 success criterion #5 ("Contact, newsletter, and press-list submissions arrive at three distinct Formspree inboxes") satisfied via the three new params.
- All five tasks pass acceptance criteria.
- Plan is correctly marked `autonomous: false` due to Task 1's maintainer-action requirement.
</success_criteria>

<output>
After completion, create `.planning/phases/02-operational-safety/02-02-SUMMARY.md` with:
- Whether real Formspree IDs are in place or `REPLACE_WITH_*` placeholders remain (gated state)
- All four `<form action=>` rendered URLs (extract from `public/` after build)
- The verbatim consent-label strings rendered into the three pages (proof of D-04/D-05 fidelity)
- Background-class decision recorded for each consent label (light-surface vs dark-section variant)
- Whether the legacy `formspreeId` was successfully removed from rendered output
- Any deviations from PATTERNS §3 (e.g. layout restructuring choices)
</output>
