public/
resources/
.hugo_build.lock
.reference/
*.png
!static/favicon-32x32.png
!static/apple-touch-icon.png
.playwright-mcp/
.claude/

# Phase 4 BUILD-01 — Tailwind compile output (generated in CI from assets/css/main.css)
assets/css/compiled.css
# Tailwind standalone CLI binary, downloaded locally for two-terminal dev workflow
# (see docs/MAINTENANCE.md). Never committed; CI re-downloads from pinned release.
/tailwindcss
